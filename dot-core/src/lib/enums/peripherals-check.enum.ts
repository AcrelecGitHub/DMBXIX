export enum PeripheralsCheckMode {
    Ignore = '-1',
    Simulate = '0',
    Mandatory = '1',
    Optional = '2'
}

export enum PeripheralsErrorsTypes {
    PrinterError = 'PrinterError',
    PaymentError = 'PaymentError',
    ScannerError = 'ScannerError',
    POSError = 'POSError'
}
